PORT=3000

# Set this on Render so the app uses PostgreSQL instead of the local JSON fallback.
DATABASE_URL=
OLLAMA_API_KEY=
OLLAMA_MODEL=gpt-oss:20b
OLLAMA_API_URL=https://ollama.com/api/chat

# Optional: openFDA works without a key for light use, but a key can raise quota.
OPENFDA_API_KEY=
