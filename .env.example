# Sao chép file này thành .env rồi điền khóa thật vào.
#   cp .env.example .env
# File .env đã nằm trong .gitignore — không bao giờ commit khóa lên git.

# Ollama Cloud — dùng cho starter-code/clinical_coding_prototype.py
OLLAMA_API_KEY=

# Google Gemini — dùng cho starter-code/prompt_prototype.py
GEMINI_API_KEY=
